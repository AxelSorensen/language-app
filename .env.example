# OpenAI API key used by the server-side grammar-check/journaling AI features
# (read in server/plugins/llm.ts and app/repositories/OpenAIRepository.ts).
# Without it, the OpenAI repository falls back to mock responses.
OPENAI_API_KEY=sk-your-openai-api-key
